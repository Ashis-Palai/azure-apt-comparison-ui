{
  "name": "apt-comparison-ui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "axios": "^1.5.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -r https://x-access-token:${GITHUB_TOKEN}@github.com/Ashis-Palai/apt-comparison-ui.git -u \"github-actions <actions@github.com>\""
  },
  "homepage": "https://Ashis-Palai.github.io/apt-comparison-ui",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
